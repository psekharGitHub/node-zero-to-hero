// const _ = require('lodash') //external dependencies needs to be installed before using them

// const array = [1, [2, [3, [4]]]]
// const flattened_array = _.flattenDeep(array)    //flattens the array

// console.log(flattened_array)
// console.log('Hello People')

const num1 = 5
const num2 = 10

function addValues() {
    console.log(`The sum is ${num1 + num2}`);
}

addValues()